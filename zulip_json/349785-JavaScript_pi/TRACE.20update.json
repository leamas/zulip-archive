[
    {
        "content": "<p>For information only, I have rewritten the TRACE feature from next build JS v2.0 alpha 4.<br>\n(1)  whether or not tracing is compiled in. now depends on <br>\n<code>#define TRACE_YES  true  // if true, include TRACE code. Should be false in release</code><br>\nIf false, only <code>((void)0)</code> is compiled (needed because of the following ; )<br>\nThis has silenced the numerous warnings from the compiler.<br>\n(2) <code>TRACE(0,   ... </code> is always traced, otherwise tracing is now between two limits</p>\n<div class=\"codehilite\"><pre><span></span><code>#define TRACE_MAX   30  // TRACE level range   (Level 0 always traced)\n#define TRACE_MIN   25\n</code></pre></div>\n<p>which helps make tracing specific to the area of interest</p>",
        "id": 320703097,
        "sender_full_name": "Tony Voss",
        "timestamp": 1673443961
    }
]