[
    {
        "content": "<p>There is the bundled library plugindc that simplifies drawing by providing a simple abstraction layer. It allows the developer to use its methods to draw using either OpenGL (wxGLCanvas) or non-OpenGL (wxDC). For OpenGL the abstraction means a developer can write:</p>\n<div class=\"codehilite\"><pre><span></span><code>Drawrectangle(x, y, width, height);\n</code></pre></div>\n<p>insead of low level complex wxGLCanvas API's</p>\n<div class=\"codehilite\"><pre><span></span><code>glBegin(GL_QUADS);\nglVertex2i(x, y);\nglVertex2i(x + width, y);\nglVertex2i(x + width, y + height);\nglVertex2i(x, y + height);\nglEnd();\n</code></pre></div>\n<p>However most plugins seem to implement a class that inherits from plugindc that provides a couple of members and methods. Eg from DashboardSK</p>\n<div class=\"codehilite\"><pre><span></span><code>class dskDC : public piDC {\nprivate:\n    /// Scale factor of the context\n    double m_scale_factor = 1.0;\n    bool m_is_gl;\n</code></pre></div>\n<p>with constructors for either wxGLCanvas or wxDC and getters/setters for scale factor etc.</p>\n<p>What is the reason or the requirement for this inherited class ?</p>",
        "id": 593053187,
        "sender_full_name": "TwoCan",
        "timestamp": 1777998752
    },
    {
        "content": "<p>This might be far out there in the future... but I feel like at this stage maybe opencpn should target only opengl...    the ability to compile with emscripten would be interesting as well</p>",
        "id": 593286200,
        "sender_full_name": "seandepagnier",
        "timestamp": 1778080039
    }
]